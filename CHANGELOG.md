# Changelog

## Unreleased

- Replaced the untyped layout loader with ordered `Source` and `WatchSource` contracts.
- Added typed immutable snapshots and generic managers with validation-before-publication.
- Added strict YAML file, byte, map and prefixed environment sources.
- Added exact-key etcd runtime configuration loading and watching.
- Removed the legacy `Layout`, untyped getters, placeholder resolver and string watcher APIs.
