# hexas-config AI 协作与工程约束

本文件适用于 hexas-config 整个仓库。它参考 FF-Center/Server 的安全与交付要求，并按“进程启动配置库”的边界裁剪。若文档与可执行代码、测试或 CI 不一致，以可执行行为为准，并在同一改动中修正文档。

## 1. 工作原则

- 开始前读取相关实现、测试、README 和 `git status --short`，先确认公开 API 与未提交改动。
- 只修改任务直接涉及的内容；优先修正现有实现，不建立同义的第二套 Loader、Getter 或 Watcher。
- 默认兼容演进公开 API、配置键、环境变量映射与目录约定；用户明确授权破坏性重构时，以职责单一和边界清晰为优先，并在交付中列出迁移影响。
- 生成内容必须从唯一源重新生成，不直接手改生成产物。本仓当前没有生成代码目录；新增时必须同时记录生成入口。
- 未经用户明确要求，不执行 `git add`、`git commit`、`git push`、创建分支或 PR。

## 2. 配置模型与唯一源

- `template.yaml` 是静态结构和安全默认值的唯一源；`overlay.yaml` 只表达环境差异，不复制完整模板。
- `Layout` 是文件位置与占位符文件顺序的显式描述；`DirectoryLayout` 是默认目录约定，不在 Loader 内另行推导路径。
- 进程环境与显式运行时覆盖只影响加载结果，不反写配置文件；命令行解析由调用方负责。
- `replace.yaml`、`global.yaml` 和 `.env` 不得保存生产秘密；`.env` 仅用于本地开发且不应提交。
- 配置优先级必须保持确定、可测试并在 README 中同步说明。修改优先级属于兼容性变更。
- FF-Center 的 project client 游戏配置不属于本库。不得把草稿、发布、回滚、revision 或客户端灰度规则塞入进程配置 Loader。

## 3. 安全与隐私底线

- 不在代码、样例、测试快照、错误、日志或元数据中写入真实密钥、私钥、完整 token、DSN、receipt 或个人信息。
- `DumpTo` 默认脱敏不得被弱化；新增敏感命名时扩充测试和 README。原始 `Settings` 只能供受控的进程内逻辑使用。
- 不把客户端输入或远端配置当作可信边界。认证、支付、发货、资产授权和 project 隔离仍由消费服务校验。
- 本库不得执行远端秘密拉取、无限重试或无 timeout 的网络调用。若未来增加远端 Source，必须显式注入客户端、正数 timeout、有限重试和取消上下文。
- 动态变量只提供非敏感运行信息，不能生成身份凭证或授权结论。

## 4. 加载、校验与并发

- Loader 只构建隔离的只读 `Snapshot`，不修改 Manager 或任何全局状态。
- Manager 必须在新快照完成文件读取、合并、占位符解析、类型解码和业务校验后再原子发布。
- 任一阶段失败必须保留旧快照，不通知 Watcher，不产生半更新状态。
- 占位符必须检测循环；缺失值返回包含键名但不包含秘密值的错误。
- Watcher 注册表必须有并发保护。不得在持有 Watcher、Manager 或内存覆盖锁时调用外部回调。
- Watcher 回调同步执行，应保持短小；新增异步能力时必须有有界队列、退出机制和背压策略。
- 内存覆盖是临时运维能力，正式文件重载时必须清除，不能成为事实唯一来源。

## 5. Go 与 API 规则

- 最低兼容 Go 版本为 1.24.12；同时保持在 FF-Center/Server 固定的 Go 1.26.7 上可构建。
- 错误使用 `%w` 保留根因；库代码不 `panic`。
- 公开 API 以 `Layout`、`Loader`、`Snapshot`、`Metadata`、`Manager` 和 `Watcher` 为边界，不增加包级可变配置或同义入口。
- 不把仅由加载流水线使用的 Resolver、注册表或存储实现暴露为公共扩展点；确需扩展时先定义确定的生命周期、并发和优先级语义。
- Go 文件和公开标识符使用英文命名。配置语义、公开 API、设计背景、优先级和安全边界统一维护在 README 或本文档，不在源码中维护重复注释。

## 6. 测试与验证

每次代码改动至少运行：

```bash
go fmt ./...
go vet ./...
go test -race ./...
git diff --check
```

按改动面补充以下测试：

- Loader：模板、overlay、环境变量和显式运行时覆盖优先级；缺失文件与解析失败。
- Placeholder：完整值类型、嵌入字符串、缺失值、环境覆盖和循环检测。
- Reload：失败保留旧快照，成功后才通知，内存覆盖清理。
- Watcher：取消订阅、回调重入、并发通知；测试不得依赖 map 遍历顺序。
- Security：默认敏感键和自定义敏感键均不出现在 Dump。
- Snapshot：集合与元数据不可从外部修改，来源信息与脱敏输出正确。

文档或 AI 规则单独变更仍需运行 `git diff --check`，并人工检查路径、链接、示例命令和优先级描述。

## 7. 交付说明

交付时说明：改动内容、关键设计选择、兼容性、执行的验证、未执行项目、剩余风险和回滚方法。若修改配置优先级、公开 API、敏感值处理或重载语义，必须单独标明迁移影响。
